{
  "name": "@guard/capability-gateway",
  "version": "0.0.0",
  "private": true,
  "description": "Strict capability normalization, pinned policy authorization, and one-use dispatch.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --build",
    "test": "npm run build && node --test --test-reporter=../../scripts/deterministic-test-reporter.mjs dist/*.test.js"
  },
  "dependencies": {
    "@guard/contracts": "0.0.0",
    "@guard/policy-engine": "0.0.0",
    "@guard/schema-validation": "0.0.0"
  }
}
