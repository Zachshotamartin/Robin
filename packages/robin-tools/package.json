{
  "name": "@guard/robin-tools",
  "version": "0.0.0",
  "private": true,
  "description": "Versioned Robin coding-tool contracts and real-tool capability composition.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --build",
    "test": "npm run build && node --test --test-reporter=../../scripts/deterministic-test-reporter.mjs dist/*.test.js"
  },
  "dependencies": {
    "@guard/capability-gateway": "0.0.0",
    "@guard/context-broker": "0.0.0",
    "@guard/contracts": "0.0.0",
    "@guard/tool-git": "0.0.0",
    "@guard/tool-process": "0.0.0",
    "@guard/tool-workspace": "0.0.0"
  }
}
