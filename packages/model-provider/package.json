{
  "name": "@guard/model-provider",
  "version": "0.0.0",
  "private": true,
  "description": "Provider-neutral model port and deterministic synthetic provider for Robin.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --build",
    "test": "npm run build && node --test dist/*.test.js"
  },
  "dependencies": {
    "@guard/contracts": "0.0.0"
  }
}
