{
  "name": "@guard/schema-validation",
  "version": "0.0.0",
  "private": true,
  "description": "Shared trusted JSON Schema compilation and immutable value validation boundary.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "typecheck": "tsc --build --force",
    "test": "npm run build && node --test dist/*.test.js"
  },
  "dependencies": {
    "@guard/contracts": "0.0.0",
    "ajv": "8.20.0"
  }
}
