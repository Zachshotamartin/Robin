{
  "name": "@guard/milestone-a-scenarios",
  "version": "0.0.0",
  "private": true,
  "description": "Deterministic end-to-end Milestone A scenario evidence for the Guarded Agent runtime.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "fixtures"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "typecheck": "tsc --build --force",
    "test": "npm run build && node --test dist/*.test.js"
  },
  "dependencies": {
    "@guard/agent-driver": "0.0.0",
    "@guard/capability-gateway": "0.0.0",
    "@guard/capability-repository": "0.0.0",
    "@guard/capability-synthetic": "0.0.0",
    "@guard/context-broker": "0.0.0",
    "@guard/contracts": "0.0.0",
    "@guard/event-store": "0.0.0",
    "@guard/policy-engine": "0.0.0",
    "@guard/profile-registry": "0.0.0",
    "@guard/runtime-host": "0.0.0"
  }
}
