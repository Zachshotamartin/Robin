{
  "name": "@guard/profile-registry",
  "version": "0.0.0",
  "private": true,
  "description": "Immutable validated task-profile registry for Robin's internal kernel.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "typecheck": "tsc --build",
    "test": "npm run build && node --test --test-reporter=../../scripts/deterministic-test-reporter.mjs dist/*.test.js"
  },
  "dependencies": {
    "@guard/contracts": "0.0.0",
    "@guard/schema-validation": "0.0.0"
  }
}
