{
  "name": "@guard/context-broker",
  "version": "0.0.0",
  "private": true,
  "description": "Generic bounded context-source port, registry, and deterministic in-memory source.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --build --force",
    "test": "npm run build && node --test dist/*.test.js"
  },
  "dependencies": {
    "@guard/contracts": "0.0.0",
    "@guard/policy-engine": "0.0.0"
  }
}
